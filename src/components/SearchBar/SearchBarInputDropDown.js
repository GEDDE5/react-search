import React from 'react'
import PropTypes from 'prop-types'

const SearchBarInputDropDown = ({ items }) => (
  <div></div>
)

SearchBarInputDropDown.propTypes = {
}

export default SearchBarInputDropDown
